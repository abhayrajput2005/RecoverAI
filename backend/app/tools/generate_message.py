"""Tool: generate_message — Draft concise customer-facing recovery text."""


def generate_message(case_id: str, action: str, context: dict) -> str:
    """
    TODO (Day 4-6): generate a short, non-generic customer message for the
    chosen recovery action (e.g. payment-link nudge). LLM-generated text is
    fine here since it's not an executable action itself.
    """
    raise NotImplementedError
